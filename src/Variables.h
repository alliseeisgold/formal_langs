#pragma once
#include <vector>

using std::vector;

const vector<char> Alphabet = {'a', 'b', 'c', '1'};
const vector<char> Operations = {'.', '*', '+'};
const vector<char> Multiplier = {'*'};
const char Empty = '1';
const size_t NOT_FOUND = 1e9;

